#Parameters for node

# erase from_path parameter if you dont run it from Moosmann's dataset.
from_path: "/home/zzz/ros/algorithms/data/scenario1" 
# lidar_config_link: relative path to config file. Example: "/config/projection/vlp16.cfg"

# node: {
#   laser_frame_id: ros laser frame_id
#   topic_clouds: topic to subscribe
#   num_thread: num of threads
# }
node: {
  laser_frame_id: "world",
  topic_clouds: "",
  num_thread: 1
}
# ground_remover: {
#   1.     
    # remover: "depth_ground_remover",
    # angle: ground_remove_angle,
    # smooth_window_size: argument for picture smoother
#   2.
    # remover: "tunnel_ground_remover",
    # smooth_window_size: 7.0,
    # height: 
    # sensor_height: 
# }
ground_remover: {
  remover: "depth_ground_remover",
  angle: 5.0,
  smooth_window_size: 5
}
# ground_remover: {
#   remover: "tunnel_ground_remover",
#   smooth_window_size: 5,
#   height: 0.3,
#   sensor_height: 1.73
# }

# clusterer: {
#   angle_tollerance: 10,
#   min_cluster_size: 20,
#   max_cluster_size: 100000
# }
clusterer: {
  angle_tollerance: 10,
  min_cluster_size: 20,
  max_cluster_size: 100000
}

# cloud_publisher: {
#   topic_name: "segmented_cloud"
# }
cloud_publisher: {
  topic_name: "segmented_cloud"
}

# objects_publisher: {
#   topic_name: for publisher,
#   use_axial_bounding_box: 
# }
objects_publisher: {
  topic_name: "segmented_objects",
  use_axial_bounding_box: false
}


